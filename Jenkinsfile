pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'First Pipeline Github'
                sh '''
                cat raw.txt
                '''
            }
        }
    }
}
